#
# Payment connectors — pluggable payment configuration models
#
# Connectors are the merchant-configuration side (settings UI).
# PaymentMethods (in invoicing/payment_methods.py) are the checkout-logic side.
#

# EOF
